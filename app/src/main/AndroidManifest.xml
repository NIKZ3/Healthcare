<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.t1">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".insertreveiw"></activity>
        <activity android:name=".myappointment" />
        <activity android:name=".docdetail" />
        <activity android:name=".doclist" />
        <activity android:name=".plogin" />
        <activity
            android:name=".patientinitial"
            android:label="@string/title_activity_patientinitial"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".pregister_activity" />
        <activity android:name=".p_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>