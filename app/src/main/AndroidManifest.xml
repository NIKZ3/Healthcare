<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.t1">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StepCounter"></activity>
        <activity android:name=".Emergency" />
        <activity android:name=".getdoclocation" />
        <activity android:name=".doclogin" />
        <activity android:name=".docgetdetails" />
        <activity
            android:name=".dochome"
            android:label="Medically YOURS"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".d_activity" />
        <activity android:name=".doc_register" />
        <activity android:name=".docaddress" />
        <activity android:name=".app_list" />
        <activity android:name=".review_list" />
        <activity android:name=".patreview_list"/>
        <activity android:name=".appdetail" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".insertreveiw" />
        <activity android:name=".myappointment" />
        <activity android:name=".docdetail" />
        <activity android:name=".doclist" />
        <activity android:name=".plogin" />
        <activity
            android:name=".patientinitial"
            android:label="Medically YOURS"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".pregister_activity" />
        <activity android:name=".p_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>